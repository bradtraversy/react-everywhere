{
  "name": "@react-everywhere/email",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/render.jsx --bundle --platform=node --format=esm --jsx=automatic --packages=external --outfile=dist/render.js",
    "dev": "npm run build && node dist/render.js"
  },
  "dependencies": {
    "@react-email/components": "^1.0.12",
    "@react-email/render": "^2.1.0",
    "@react-everywhere/logic": "*",
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
